general:
  buildTool: 'docker'
  gitSshKeyCredentialsId: github_ssh
  productiveBranch: 'kyma/app'
steps:
  buildExecute: 
    dockerImageName: cloud-cap-risk-management
  artifactPrepareVersion:
    dockerVersionSource: custom
    filePath: cfg/VERSION  
  
